# Prepare Release 1.0.3 — 2026-07-14 22:25

## What was requested

Bump `@sdcorejs/angular-material-datetime` lên `1.0.3`, commit/push branch đã verify và tạo pull request vào `main`. Không publish npm trong bước này.

## What was changed

- EDIT `projects/datetime/package.json` — bump package version từ `1.0.2` lên `1.0.3`.
- EDIT `projects/datetime/CHANGELOG.md` — thêm patch release entry cho APF packaging và hardening work.
- EDIT `package-lock.json` — đồng bộ workspace package version `1.0.3`.
- DELETE `.changeset/release-1-0-3.md` — changeset đã được consume bởi `changeset version`.
- EDIT `.gitignore` — giữ `.sdcorejs/tasks/current-session.md` là local resume checkpoint, không đưa live state vào PR.

## Decisions made

- Release type là patch (`1.0.3`).
- Package chỉ publish từ `dist/datetime`; source project không phải publish target.
- Không tạo tag, GitHub release hoặc chạy `npm publish` khi chưa có xác nhận riêng.
- PR bao gồm toàn bộ library/package/showcase/docs changes đã được yêu cầu trên branch `codex/release-1.0.3`.

## Verification

- `npm test -- --runInBand` — pass 15 suites, 149 tests.
- `npm run lint` — pass datetime, moment-adapter, date-fns-adapter và demo.
- `npm run build:all` — pass cả ba Angular Package Format libraries.
- Pages production build — pass với base href `/angular-material-datetime/`.
- `npm run test:package` — pass cho `@sdcorejs/angular-material-datetime@1.0.3`.
- `npm run pack:datetime` — pass; 23 files, FESM/declarations present, source/spec/tsconfig absent.
- Clean Angular 19 consumer — tarball install, FESM resolution và production build pass.

## Open questions / follow-ups

- GitHub CLI chưa authenticated trong môi trường hiện tại; PR API cần `gh auth login` nếu Git credentials không cung cấp cách khác.
- Social preview crawler re-scrape vẫn thực hiện sau GitHub Pages deployment.
- npm publish vẫn chờ project owner xác nhận riêng.

## Next suggested action

- Review và merge PR vào `main` sau khi CI xanh.
- Sau merge, xác nhận riêng trước khi chạy `npm publish ./dist/datetime --access public`.

## Skill provenance

Skills invoked: `sdcorejs-ship` -> `sdcorejs-git`.
